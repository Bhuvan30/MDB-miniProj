<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Appointment System</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        margin: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        height: 100vh;
      }

      .landing-container {
        background-color: #fff;
        border: 1px solid #ccc;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        width: 400px;
        text-align: center;
      }

      .landing-container h2 {
        color: #333;
      }

      .selection-form {
        display: flex;
        justify-content: space-around;
        margin-top: 20px;
      }

      .landing-buttons input {
        display: block;
        padding: 10px;
        width: 150px;
        text-decoration: none;
        color: #fff;
        border-radius: 4px;
        cursor: pointer;
        border: none;
      }

      .take-appointment {
        background-color: #4caf50;
      }

      .view-appointments {
        background-color: #3498db;
      }
    </style>
  </head>
  <body>
    <div class="landing-container">
      <h2>Welcome</h2>

      <div class="landing-buttons">
        <form class="selection-form" action="/neworview" method="post">
          <input
            type="submit"
            name="appointmentType"
            value="Take Appointments"
            class="take-appointment"
          />

          <input
            type="submit"
            name="appointmentType"
            value="View Appointments"
            class="view-appointments"
          />
        </form>
      </div>
    </div>
  </body>
</html>
